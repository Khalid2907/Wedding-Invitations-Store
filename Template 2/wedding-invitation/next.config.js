/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add any custom Next.js configuration here.
  // The bundle analyzer is disabled by default.
  // If you ever need it, you can enable it via an environment variable.
};

module.exports = nextConfig;
